{
  "name": "awesome-blog",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "git subtree push --prefix dist blog-app1 gh-pages || echo 'If first time, creating gh-pages branch' && git push blog-app1 `git subtree split --prefix dist main`:gh-pages --force"
  },
  "dependencies": {
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.8.2",
    "react": "^19.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
